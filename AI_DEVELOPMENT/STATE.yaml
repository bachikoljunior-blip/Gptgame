# The hand-written half of the record: judgement, plan, blockers, next action.
#
# Measurements are not written here. They live in AI_DEVELOPMENT/MEASURED.md, produced by
# `npm run measure` and sealed with a digest. Where the two disagree, MEASURED.md is right.
#
# Verdicts use three words and no others: satisfied | not satisfied | not measured.
# Round history that is no longer needed to continue lives in PROJECT_MEMORY.md.

project:
  id: "bachikoljunior-blip/Gptgame"
  product: "ECHO//SEVEN mobile-first browser game"
  canonical_product_source: "index.html"
  public_url: "https://bachikoljunior-blip.github.io/Gptgame/"

work:
  unit: "ends when a blind comparison the agent launched completes; everything up to there is the agent's"
  units_requested: 1   # the user's dial. A number, or "continuous". Nothing else about pacing is set here:
                       # what to repair, in what order, and when a comparison is worth launching are the agent's.

logical_session:
  id: "gptgame-continuation-2026-08-01"
  active: true
  explicit_end_required: true

objective:
  id: "iphone-se3-automation-2026-08-01"
  statement: "Replace the routine physical-device release check with free automated iPhone SE (3rd generation) browser gates: Playwright WebKit for every product change and iOS Simulator Mobile Safari before Pages deployment, then merge and verify the delivery."
  status: "not satisfied"
  open:
    - "not satisfied: the corrected target WebKit rerun on PR #28 has not passed"
    - "not measured: iOS Simulator Mobile Safari is main-only and cannot run until PR #28 merges"
    - "split by user instruction: what the phone gates measure is judged normally; what can be inferred from those numbers is inferred and the inference is judged against the criterion; sustained thermals, speaker acoustics, device audio latency and haptic feel are out of scope"

reference_benchmark:
  path: "AI_DEVELOPMENT/REFERENCE_BENCHMARKS.md"
  status: "not measured"
  note: "The rules are in the file itself and in AGENTS.md; not restated here. Nothing below that file's new section has been re-derived under it yet."
  open:
    - "not measured: the eight elements have not been re-derived as parts of the concept | route: derive them from the concept at a granularity that can be compared; needs nothing not already here"
    - "not measured: the element set has never been read back against the concept as a set, so whether anything the concept asks for is uncovered is unknown | route: name which element covers each part of the concept; more than the minimum is fine, missing is not"
    - "not measured: the four references have never been checked against the four selection axes | route: check each title against each axis from published material, name a replacement where one fails"
    - "not measured: no rule exists for turning several judges answers into one verdict; the critic skill covers findings only | route: decide it before the first comparison runs, not after seeing its answers"
    - "not measured: no blind comparison has been run for any element | route: our side is already capturable at matched framings; reference material and a judge who has not seen the build are both obtainable and neither has been requested"

execution:
  active_work: "PR #28 carries the reconciled target-browser gates on origin/main ea0a90e. Quality passed; the first target WebKit run completed all product checks and stopped for review of the missing baseline plus a runner-only touch-point diagnostic. The inspected baseline and descriptor-based touch contract are prepared for the corrected rerun."
  last_verified_checkpoint:
    revision: "ea0a90e9c53a20d45c5caf3dfb829e9a1078f17e"
    product_artifact: "69f72f7b1c8ee9a6b6603687d10d838376384df0741abec70e51edcb9606a6f7"
    build_id: "2026.08.01-k"
    branch: "origin/main"
  modified_but_unverified:
    - "AGENTS.md, AI_DEVELOPMENT/STATE.yaml, PROJECT_MEMORY.md — goal-only instruction rewrite"
    - "scripts/measure.mjs, scripts/measured-digest.mjs, scripts/verify-continuity.mjs, scripts/verify-floor.mjs, package.json"
    - ".github/workflows/pages.yml, .github/workflows/quality.yml, tools/test-iphone-webkit.mjs, tools/test-ios-safari.mjs, tests/baselines/iphone-se3-webkit-loop2.png — the open PR #28 work"
  blockers:
    - "The corrected target WebKit branch rerun must pass before PR #28 can merge."
    - "iOS Simulator Mobile Safari is deliberately main-only and cannot execute until the passing PR is merged."
  unmeasured:
    - "not measured: workload-to-phone-budget, memory footprint and reach are inferable from the gates but no inference has been written yet | route: state each inference with the numbers it rests on and judge it against the criterion"
    - "Exact target captures use headless Chromium with software WebGL; Japanese glyphs were unavailable in that browser, so geometry and font-size metrics hold but glyph appearance does not."
    - "The connected GitHub tools expose no branch-protection or ruleset mutation, and local gh is absent. Nothing blocks a merge or a publish; the sealed MEASURED.md is what stands in for that."
    - "GitHub's legacy branch-based Pages publisher cannot be disabled with the exposed connector; the available defence is running the tested custom deployment after every main push."
  exact_next_action: "Run the once-only half: derive the elements as parts of the current concept at a granularity that can be compared, read the finished set back against the concept and name which element covers each part of it, then check each reference against the four axes. This half does not run again until the concept changes. The PR #28 WebKit rerun is unrelated and still open."

recovery:
  last_known_good: "source ea0a90e9c53a20d45c5caf3dfb829e9a1078f17e / public artifact 69f72f7b1c8ee9a6b6603687d10d838376384df0741abec70e51edcb9606a6f7"
  method: "Revert the task branch to origin/main ea0a90e before merge; after merge use a revert PR. The Pages workflow restores the verified build-k artifact after a failed candidate deployment."

remote:
  working_branch: "claude/repo-instructions-constraints-r0070m"
  main_revision: "ea0a90e9c53a20d45c5caf3dfb829e9a1078f17e"
  deployed_artifact: "69f72f7b1c8ee9a6b6603687d10d838376384df0741abec70e51edcb9606a6f7 (build 2026.08.01-k)"
  open_pull_requests: [28]

unattended_allowed: false

updated_at: "2026-08-02"
